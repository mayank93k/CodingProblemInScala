package main.scala.org.coding.questions.mayank

object arrayLength {
  /**
    * Program to print the number of elements present in an array.
    */
  def main(args:Array[String]): Unit ={
  val arr:Array[Int]=Array(1,2,3,4,5,6,7,8)
  println("The length of array is: "+arr.length)
}
}
